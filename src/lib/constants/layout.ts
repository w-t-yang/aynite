import type { LayoutConfig } from './types'

// ─── Aynite Default Layout ──────────────────────────────────────────────

export const AYNITE_LAYOUT: LayoutConfig = {
  id: 'aynite-default',
  name: 'Default',
  layout: {
    id: 'tile-home',
    type: 'leaf',
    name: 'home',
    size: 100,
  },
}
