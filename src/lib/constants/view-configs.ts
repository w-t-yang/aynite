/**
 * View configuration has been migrated from TypeScript constants to JSON files.
 *
 * Each view's config.json lives alongside its source code at:
 *   src/renderer/views/<view>/config.json
 *
 * These are bundled with the view build and synced to:
 *   ~/.aynite/views/<view>/config.json
 *
 * See the Vite plugin `copyViewConfigs` in vite.views.config.ts.
 */
