// File viewer registry has been removed.
// Fileviews are now resolved dynamically via their config.json file_extensions
// in src/renderer/views/fileview-xxx/ directories.
// The file browser reads fileview configs and matches against file extensions at runtime.
export {}
